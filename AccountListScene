// Author: David Corrales Marco

import java.util.ArrayList;

import javafx.geometry.Pos;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.layout.BorderPane;
import javafx.scene.layout.GridPane;
import javafx.scene.layout.VBox;
import javafx.scene.text.Font;
import javafx.scene.text.Text;

import java.util.ArrayList;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.layout.GridPane;

public class AccountListScene extends SceneBasic
{

	GridPane display = new GridPane();//container holding ...
	
	public AccountListScene ()
	{
		super("Account List Menu");		
		
		Label labelU = new Label("Username:");//label for username column
		Label labelA = new Label("Account: ");//label for account column
		Label blank = new Label("\t");
		display.add(labelU,0,0);
		display.add(labelA,2,0);
		display.add(blank,1,0);
		

		layout.getChildren().add(display);
		Button adminMenu = new Button("AdminMenu");
		adminMenu.setOnAction(e-> SceneManager.setAdminScene());
		Button logOut = new Button("log out");
		logOut.setOnAction(e-> SceneManager.setLoginScene());
		layout.getChildren().add(adminMenu);
		layout.getChildren().add(logOut);
		
		
	}
	
	public void getAccountList() {
		
	}
}


//here to make scene's pretty later
/*
public class AccountListScene extends SceneBasic{
	
	private GridPane root;
	
	public AccountListScene(){
		super("Account List");
		
		Label title = new Label(super.getTitleText());
		title.setFont(new Font(40));
		
		//Display accounts here
		
				
		Button button1 = new Button("Admin Menu");
		button1.setOnAction(e -> SceneManager.setAdminScene());
		
		Button button2 = new Button("Log out");
		button2.setOnAction(e -> SceneManager.setLoginScene());
		
		root = new GridPane();
		root.setAlignment(Pos.CENTER);
		root.setVgap(10);
		root.setHgap(5);
		
		root.add(button1, 1, 0);
		root.add(button2, 2, 0);
		
		VBox vbox = new VBox(30, title, root);
		vbox.setAlignment(Pos.CENTER);
		
		BorderPane bp = new BorderPane(vbox);
		BorderPane.setAlignment(bp, Pos.CENTER);	
		super.scene = new Scene(bp, 400, 300);
	}
	
	public void getAccountList() {
		
	}	

}
*/
